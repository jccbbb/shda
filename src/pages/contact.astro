---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout title="Contact" description="Get in touch with the Scandinavian Hard Dance Alliance - apply for membership, ask questions, or propose collaborations.">
  <section class="page-hero">
    <div class="container">
      <h1>Get In Touch</h1>
      <p class="lead">Questions, membership inquiries, or collaboration proposals? We'd love to hear from you.</p>
    </div>
  </section>

  <section>
    <div class="container">
      <!-- Prominent Email Contact Section -->
      <div class="email-contact-section">
        <h2>Contact Us Directly</h2>
        <p class="intro-text">
          We'd love to hear from you! Send us an email and we'll get back to you within 48 hours.
        </p>

        <div class="email-display">
          <a href="mailto:hello@shda.org" class="email-link">hello@shda.org</a>
        </div>

        <div class="contact-guidance">
          <h3>When You Contact Us</h3>
          <div class="guidance-grid">
            <div class="guidance-item">
              <h4>Membership Applications</h4>
              <p>Include your organization name, location, website, and a brief description of your activities.</p>
            </div>
            <div class="guidance-item">
              <h4>Event Submissions</h4>
              <p>Provide event name, date, venue, city, lineup information, and ticket link.</p>
            </div>
            <div class="guidance-item">
              <h4>Partnership Proposals</h4>
              <p>Describe your proposal and how it aligns with SHDA's values and mission.</p>
            </div>
            <div class="guidance-item">
              <h4>General Inquiries</h4>
              <p>Feel free to ask us anything about the alliance, our members, or hard dance events.</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Contact Information Cards -->
      <div class="contact-info-cards">
        <div class="info-card">
          <h3>Follow Us</h3>
          <p>Stay updated with the latest news and events:</p>
          <div class="social-links">
            <!-- TODO: Add actual social media links -->
            <a href="#" class="social-link">Facebook</a>
            <a href="#" class="social-link">Instagram</a>
            <a href="#" class="social-link">Twitter/X</a>
          </div>
        </div>

        <div class="info-card">
          <h3>Member Organizations</h3>
          <p>
            You can also reach out directly to our <a href={`/members`}>member organizations</a>
            for event-specific inquiries or collaborations.
          </p>
        </div>
      </div>

      <!-- Quick Links -->
      <div class="quick-links-section">
        <h2>Looking for Something Specific?</h2>
        <div class="quick-links-grid">
          <div class="quick-link-card">
            <h3>Join SHDA</h3>
            <p>
              Learn about membership benefits and how to apply to become part of the alliance.
            </p>
            <a href={`/members#join`} class="btn btn-secondary">Membership Info</a>
          </div>
          <div class="quick-link-card">
            <h3>Submit an Event</h3>
            <p>
              Member organizations can submit events to be featured on our calendar.
            </p>
            <a href={`/events#submit`} class="btn btn-secondary">Submit Event</a>
          </div>
          <div class="quick-link-card">
            <h3>Press & Media</h3>
            <p>
              Media inquiries, press releases, and official alliance information.
            </p>
            <a href="mailto:hello@shda.org" class="btn btn-secondary">Media Contact</a>
          </div>
          <div class="quick-link-card">
            <h3>Partnerships</h3>
            <p>
              Interested in partnering with SHDA? Let's discuss collaboration opportunities.
            </p>
            <a href="mailto:hello@shda.org" class="btn btn-secondary">Partner With Us</a>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-hero {
    padding: var(--spacing-4xl) 0 var(--spacing-2xl);
    text-align: center;
    background: linear-gradient(180deg, var(--color-bg-primary) 0%, var(--color-bg-secondary) 100%);
  }

  .page-hero h1 {
    font-size: 3.5rem;
    margin-bottom: var(--spacing-md);
  }

  .page-hero .lead {
    color: var(--color-accent-cyan);
    font-size: 1.25rem;
    max-width: 700px;
    margin: 0 auto;
  }

  .email-contact-section {
    max-width: 900px;
    margin: 0 auto var(--spacing-4xl);
    text-align: center;
  }

  .email-contact-section h2 {
    color: var(--color-accent-cyan);
    font-size: 2.5rem;
    margin-bottom: var(--spacing-lg);
  }

  .intro-text {
    font-size: 1.125rem;
    line-height: 1.8;
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-2xl);
  }

  .email-display {
    background: linear-gradient(135deg, rgba(0, 240, 255, 0.1) 0%, rgba(157, 0, 255, 0.1) 100%);
    border: 2px solid var(--color-accent-cyan);
    border-radius: var(--radius-lg);
    padding: var(--spacing-3xl);
    margin-bottom: var(--spacing-3xl);
    transition: all var(--transition-base);
  }

  .email-display:hover {
    border-color: var(--color-accent-purple);
    box-shadow: 0 0 30px rgba(0, 240, 255, 0.3);
  }

  .email-link {
    color: var(--color-text-white);
    font-family: var(--font-heading);
    font-size: 2rem;
    font-weight: 700;
    text-decoration: none;
    letter-spacing: 0.02em;
    transition: all var(--transition-base);
    display: inline-block;
  }

  .email-link:hover {
    color: var(--color-accent-cyan);
    transform: scale(1.05);
  }

  .contact-guidance {
    margin-top: var(--spacing-3xl);
    text-align: left;
  }

  .contact-guidance h3 {
    color: var(--color-accent-cyan);
    font-size: 1.75rem;
    text-align: center;
    margin-bottom: var(--spacing-2xl);
  }

  .guidance-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-xl);
  }

  .guidance-item h4 {
    color: var(--color-text-white);
    font-size: 1.125rem;
    margin-bottom: var(--spacing-sm);
  }

  .guidance-item p {
    font-size: 0.9375rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
  }

  .contact-info-cards {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-xl);
    margin-bottom: var(--spacing-4xl);
    max-width: 900px;
    margin-left: auto;
    margin-right: auto;
  }

  .info-card {
    background-color: var(--color-bg-secondary);
    border: 1px solid rgba(0, 240, 255, 0.2);
    border-radius: var(--radius-md);
    padding: var(--spacing-xl);
    margin-bottom: var(--spacing-lg);
  }

  .info-card h3 {
    color: var(--color-accent-cyan);
    font-size: 1.25rem;
    margin-bottom: var(--spacing-md);
  }

  .info-card p {
    font-size: 0.9375rem;
    line-height: 1.8;
    color: var(--color-text-primary);
    margin-bottom: var(--spacing-md);
  }

  .info-card p:last-child {
    margin-bottom: 0;
  }

  .info-card a {
    color: var(--color-accent-cyan);
  }

  .social-links {
    display: flex;
    gap: var(--spacing-md);
    flex-wrap: wrap;
    margin-top: var(--spacing-md);
  }

  .social-link {
    padding: var(--spacing-sm) var(--spacing-md);
    background-color: var(--color-bg-primary);
    border: 1px solid rgba(0, 240, 255, 0.3);
    border-radius: var(--radius-sm);
    font-family: var(--font-heading);
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    transition: all var(--transition-base);
  }

  .social-link:hover {
    border-color: var(--color-accent-cyan);
    background-color: rgba(0, 240, 255, 0.1);
    box-shadow: 0 0 10px rgba(0, 240, 255, 0.3);
  }

  .quick-links-section {
    padding-top: var(--spacing-4xl);
    border-top: 1px solid rgba(0, 240, 255, 0.2);
  }

  .quick-links-section h2 {
    color: var(--color-accent-cyan);
    font-size: 2rem;
    text-align: center;
    margin-bottom: var(--spacing-2xl);
  }

  .quick-links-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: var(--spacing-xl);
  }

  .quick-link-card {
    background-color: var(--color-bg-secondary);
    border: 1px solid rgba(0, 240, 255, 0.2);
    border-radius: var(--radius-md);
    padding: var(--spacing-xl);
    text-align: center;
    transition: all var(--transition-base);
    display: flex;
    flex-direction: column;
    height: 100%;
  }

  .quick-link-card:hover {
    border-color: var(--color-accent-cyan);
    box-shadow: 0 0 20px rgba(0, 240, 255, 0.2);
    transform: translateY(-4px);
  }

  .quick-link-card h3 {
    color: var(--color-text-white);
    font-size: 1.25rem;
    margin-bottom: var(--spacing-md);
  }

  .quick-link-card p {
    font-size: 0.9375rem;
    color: var(--color-text-secondary);
    margin-bottom: var(--spacing-lg);
    flex: 1;
  }

  .quick-link-card .btn {
    margin-top: auto;
  }

  @media (max-width: 968px) {
    .page-hero h1 {
      font-size: 2.5rem;
    }

    .email-contact-section h2 {
      font-size: 2rem;
    }

    .email-link {
      font-size: 1.5rem;
    }

    .guidance-grid {
      grid-template-columns: 1fr;
    }

    .contact-info-cards {
      grid-template-columns: 1fr;
    }

    .quick-links-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
